/**
 * @namespace Util
 *
 * Collection of small-ish utilities that fit nowhere else.
 *
 * @example
 *
 * Each utility lies in its own file:
 *
 * ```
 * import { imagePromise } from "gleo/Util/imagePromise.mjs";
 * import { abortableImagePromise } from "gleo/Util/abortableImagePromise.mjs";
 * ```
 *
 *
 */
