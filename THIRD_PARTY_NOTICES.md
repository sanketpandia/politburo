# Third-party notices

The reusable flight map component is designed for Leaflet and OpenStreetMap
tiles. Leaflet is not vendored in this repository. Any UI page that enables the
map must load a pinned Leaflet release and retain the required OpenStreetMap
attribution.

