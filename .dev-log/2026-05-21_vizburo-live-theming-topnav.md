# 2026-05-21 — Vizburo Live Theming + Top Nav

## Logical unit 1: Dashboard shell, active VA switch, and Live page redesign

- **Logical unit / commit intent:** Implement canonical Vizburo top navigation with active VA selector, wire authenticated active VA switching, refactor Live page rendering to injected renderer, and redesign `/dashboard/live` as list-visible/map-enhanced with mobile list/map toggle.
- **Changed files:**
  - `internal/app/app.go`
  - `internal/dashboard/handler.go`
  - `internal/flights/handler.go`
  - `internal/routes/router.go`
  - `templates/layouts/base.html`
  - `templates/partials/desktop-nav.html`
  - `templates/partials/mobile-header.html`
  - `templates/partials/mobile-drawer.html`
  - `templates/pages/live.html`
  - `templates/partials/flight-list.html`
  - `templates/partials/live-map-shell.html`
  - `templates/partials/map-legend.html`
  - `templates/partials/flight-details-panel.html`
  - `templates/partials/flight-details-sheet.html`
  - `static/js/live-flights.mjs`
  - `static/css/design-system.css`
  - `static/css/output.css` (regenerated by `npm run css:build`)
- **Reused code / patterns / components:** Reused `infra/templates.PrepareTemplateData`, session context from `auth.GetSessionData`, role-aware `.MenuItems` from `internal/platform/ui.GetMenuItems`, `infra/session.SessionService.SwitchActiveVA`, injected `infra/templates.Renderer`, and existing `static/js/flight-map.mjs` as the map primitive.
- **Logging added or affected:** Added sanitized active VA switch logs with `surface=vizburo` and safe failure messages; no session IDs, Discord IDs, or raw VA IDs are logged by the new handler.
- **Metrics added or affected:** No metrics added. Follow-up Observability agent can add bounded active-VA-switch outcome metrics through `infra/metrics.MetricsRegistry` if requested.
- **Test surface touched or still needed:** Handler route and build surfaces were validated. Unit tests remain needed for HTMX vs non-HTMX switch redirects, missing `va_id`, unavailable VA, route registration, and Live handler template data/render behavior.
- **Build/test command(s) run and status:**
  - `go test ./internal/dashboard ./internal/flights ./internal/routes ./infra/session` — passed
  - `go build -buildvcs=false -o .air_tmp/main ./cmd/server` — passed
  - `go build -o .air_tmp/vizburo ./cmd/vizburo` — passed (tracked build artifact restored afterward)
  - `npm run css:build` — passed; emitted existing Browserslist/caniuse-lite outdated warning
  - `go test ./infra/templates` — passed
  - `go test ./...` — passed
- **Deviations from plan, if any:** Exact menu/user-menu composition remains deferred per plan and user clarification. The old `mobile-fab.html` file was left in place but no longer rendered by `base.html`, avoiding stale-tree cleanup outside the slice.
- **Blast-radius notes / dependent surfaces checked:** Checked active dashboard routes, UI auth/session context, DI wiring, role-aware menu source, Live DTO shape, template renderer partial loading, active templates under `templates/**`, and CSS/JS static assets. No comrade-bot, labour-bureau, jobs, JSON API, OpenAPI, generated code, DB migrations, or env/config changes were required.
- **Live API compliance notes:** No Live API calls or cache key contracts changed. Live freshness is labeled as background-cache status rather than inventing an exact timestamp.
- **Follow-up notes:**
  - **Swagger/OpenAPI:** No OpenAPI work needed; active VA switch is an authenticated Vizburo HTML/HTMX route, not a JSON API contract.
  - **Observability:** Optional follow-up may add bounded outcome metrics/log review for active VA switching. Avoid session IDs, Discord IDs, raw VA IDs, and raw internal error strings in labels/log fields.
  - **Unit Testing:** Add focused dashboard switch handler tests, route registration tests, Live handler render/data tests, and template static checks for top-nav/mobile VA selector parity.
