type: object
x-go-type-name: AircraftLiveriesResponse
required: [errorCode, result]
properties:
  errorCode:
    type: integer
    description: Zero indicates success; non-zero values are upstream API errors.
  result:
    type: array
    items:
      $ref: ../livery-data.yaml
